import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.catalogclient',
  appName: 'catalog-client',
  webDir: 'www'
};

export default config;
